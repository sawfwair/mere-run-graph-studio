{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Native Graph Studio desktop capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "window-state:default"
  ]
}
